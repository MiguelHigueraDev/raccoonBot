'use strict';

var splitText_js = require('./splitText.js');

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
function cutText(str, length) {
  if (str.length < length)
    return str;
  const cut = splitText_js.splitText(str, length - 3);
  if (cut.length < length - 3)
    return `${cut}...`;
  return `${cut.slice(0, length - 3)}...`;
}
__name(cutText, "cutText");
/**
 * Split a text by its latest space character in a range from the character 0 to the selected one.
 * @param str The text to split.
 * @param length The length of the desired string.
 * @copyright 2019 Aura Román
 * @license Apache-2.0
 */

exports.cutText = cutText;
//# sourceMappingURL=out.js.map
//# sourceMappingURL=cutText.js.map