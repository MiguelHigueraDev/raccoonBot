'use strict';

var pieces = require('@sapphire/pieces');
var Listener_cjs = require('../../../lib/structures/Listener.cjs');
var Events_cjs = require('../../../lib/types/Events.cjs');

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var _CoreListener = class _CoreListener extends Listener_cjs.Listener {
  constructor(context) {
    super(context, { event: Events_cjs.Events.PreContextMenuCommandRun });
  }
  async run(payload) {
    const { command, interaction } = payload;
    const globalResult = await this.container.stores.get("preconditions").contextMenuRun(interaction, command, payload);
    if (globalResult.isErr()) {
      this.container.client.emit(Events_cjs.Events.ContextMenuCommandDenied, globalResult.unwrapErr(), payload);
      return;
    }
    const localResult = await command.preconditions.contextMenuRun(interaction, command, payload);
    if (localResult.isErr()) {
      this.container.client.emit(Events_cjs.Events.ContextMenuCommandDenied, localResult.unwrapErr(), payload);
      return;
    }
    this.container.client.emit(Events_cjs.Events.ContextMenuCommandAccepted, payload);
  }
};
__name(_CoreListener, "CoreListener");
var CoreListener = _CoreListener;
void pieces.container.stores.loadPiece({
  name: "CorePreContextMenuCommandRun",
  piece: CoreListener,
  store: "listeners"
});

exports.CoreListener = CoreListener;
//# sourceMappingURL=out.js.map
//# sourceMappingURL=CorePreContextMenuCommandRun.cjs.map